---
id: US-1314
title: "Játékos rövid név megjelenítése a UI-on (short_name használata)"
priority: Should Have
status: Open
dependencies: [US-1313]
complexity: M
---

# US-1314: Játékos rövid név megjelenítése a UI-on (`short_name` használata)

> Epic: Player sync / adatminőség (US-1313 follow-up)

## Háttér

Az US-1313 bevezette a `players.short_name` oszlopot és a sync service mindkét ágban (squad + stats) tölti, viszont **olvasási oldalon még sehol nincs használva** — minden szolgáltatás és UI továbbra is `players.name`-et ad vissza, ami az api-football `/players` végpontja után a hosszú anyakönyvi név (pl. „Vinícius José Paixão de Oliveira Júnior" a „Vinícius Júnior" helyett).

Érintett service-ek (jelen kódbázis):

- `packages/backend/src/services/special-predictions.service.ts` — search/listák
- `packages/backend/src/services/tournament-tips.service.ts` — torna tippek dropdown és listák
- `packages/backend/src/services/players.service.ts` — generikus player API + `toApiPlayer`
- `packages/backend/src/services/matches.service.ts` — scorer mező-betöltés (ha kerül név)
- US-1310 leszállítása után a top players insight payloadja is (akkor élő, akkor döntsünk)

Frontend felületek (audit kell, nem teljes lista):

- `MatchesView.vue` / scorer dropdown a meccs kártyán
- `MyTipsView.vue`, `TournamentTipsView.vue` (torna tipp listák)
- Special predictions search dropdown és kártyák
- Admin / leaderboard / statisztika felületek

## Story

Mint **felhasználó**, **az ismert rövid nevet** szeretném látni a játékosoknál (pl. „Rodrygo", „Vinícius Júnior"), **ne a hosszú anyakönyvi nevet**, hogy a UI áttekinthető és a megjelenített név megegyezzen azzal, amit a sportsajtóban látunk.

## Kontraktus döntés

A backend válasz **`name` mezőjének értéke hibrid szabály szerint** áll elő — egy mező marad, frontend nem törik:

```
display_name = (name szószáma ≤ 3) ? name : (short_name ?? name)
```

A szót whitespace-szel választjuk el (`/\s+/`). Indoklás:

- Rövid magyar/spanyol/portugál nevek (pl. „Agustín Giay", „Alexis Mac Allister", „Franco Mastantuono") **megmaradnak teljes formában** — a rövid `A. Giay` / `Franco Mastantuono` / stb. értelmetlenül körülményes lenne.
- Hosszú anyakönyvi nevek (pl. „Damián Emiliano Martínez Romero", „Vinícius José Paixão de Oliveira Júnior") **lerövidülnek** (`E. Martínez`, `Vinícius Júnior`).
- A `short_name` NULL esetén mindig `name` a fallback, így nem törik el a régi viselkedés ott, ahol a sync még nem futott le.

A logika közös helper-ben (`resolvePlayerDisplayName({name, shortName})`), és SQL-szinten is ugyanígy a sortoláshoz (`DISPLAY_NAME_SQL`).

**Nem vezetünk be új `displayName` mezőt** a DTO-kban — a meglévő `name` kulcs értéke változik a válaszokban.

> Kivétel: ha valahol kifejezetten a hosszú név kell (pl. játékos profil headerben), arra pluszként visszaadhatjuk a `fullName`-et — de **csak ha konkrét UI igény van**, ebben a story-ban nem.

## Elfogadási kritériumok

### Közös helper

- [x] Új modul: `packages/backend/src/services/player-display-name.ts`, exportál `resolvePlayerDisplayName({name, shortName})` függvényt a hibrid szabállyal.
- [x] Unit tesztek (7 db): ≤3 szó keep, 4+ szó rövid, NULL `shortName` fallback, üres `name`, határeset (3 vs 4 szó).

### Backend

- [x] `players.service.ts` `toApiPlayer` a helpert hívja, sortolás `DISPLAY_NAME_SQL`-re (CASE + `regexp_split_to_array(trim(name), '\s+')`).
- [x] `special-predictions.service.ts` (`getMyPredictions` playerNameMap, `upsertPrediction` player_select label) a helpert hívja.
- [x] `tournament-tips.service.ts` (`listGlobalTypesWithPredictions` playerNameMap, `upsertGlobalPrediction` player_select label) a helpert hívja.
- [x] `predictions.service.ts` scorer pick `nameSnapshot` a helpert hívja.
- [x] Audit clean: `grep -rn "players\.name" packages/backend/src` után csak a sync service marad (ott a hosszú név adatforrás).

### Frontend

- [x] Frontend kód változatlan: `player.name` mező értéke változik backend-oldalon.
- [x] Audit: a frontenden nincs `firstname|lastname` player kontextusban; `fullName` egy helyen szerepel (Supabase user metadata, nem player).

### Tesztek

- [x] Backend: 8 új teszt (helper +7, players.service +1 új a hibrid szabályra; meglévő `Vinícius Júnior` és null-fallback tesztek érintetlenek).
- [x] **963/963 backend teszt zöld**, tsc tiszta.

### Adat

- [x] Lokál coverage: 47/1613 → 853/1682 a helyi `teams.team_type` = 'national' fix után + 1 sync futtatás. Élesen `team_type` már helyes — csak admin UI sync kell.

## Mit ne csináljunk

- Ne vezessünk be új `displayName` / `fullName` mezőt a DTO-kban.
- Ne nyúljunk a `players.name` oszlophoz a DB-ben (továbbra is a hosszú anyakönyvi név forrása).
- Ne módosítsuk a sync logikát (US-1313-ban már kész).
- Ne adjunk hozzá új API endpointot.

## Általános elvek

- TDD kötelező a service módosításokra (CLAUDE.md).
- Domain logikát nem mockolunk — csak a DB IO-t.
- Egy commit egy logikai egységre (audit + service módosítás + tesztek lehet egy commit, frontend audit külön ha kell).

## Függőség

- ~~US-1313~~ (kész): `players.short_name` oszlop és sync.

## Komplexitás

M — közös helper + 4 service módosítása + SQL sortolási kifejezés + audit.
